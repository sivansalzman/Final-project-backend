import { Company } from "../types";

const getCompany = () => {
  return {
    execute: async (orgId: string, query: Object = {}) => {
      try {
      } catch (error) {
        return [];
      }
    },
  };
};

export default getCompany;
