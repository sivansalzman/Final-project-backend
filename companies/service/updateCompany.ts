import { Company } from "../types";

const updateCompany = () => {
  return {
    execute: async (orgId: string, query: Object = {}) => {
      try {
      } catch (error) {
        return [];
      }
    },
  };
};

export default updateCompany;
