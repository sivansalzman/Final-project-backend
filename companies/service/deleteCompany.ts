import { Company } from "../types";

const deleteCompany = () => {
  return {
    execute: async (orgId: string, query: Object = {}) => {
      try {
      } catch (error) {
        return [];
      }
    },
  };
};

export default deleteCompany;
