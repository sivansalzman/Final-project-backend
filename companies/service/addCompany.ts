import { Company } from "../types";

const addCompany = () => {
  return {
    execute: async (orgId: string, query: Object = {}) => {
      try {
      } catch (error) {
        return [];
      }
    },
  };
};

export default addCompany;
