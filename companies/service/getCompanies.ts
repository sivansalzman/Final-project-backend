import { Company } from "../types";

const getCompanies = () => {
  return {
    execute: async (orgId: string, query: Object = {}) => {
      try {
      } catch (error) {
        return [];
      }
    },
  };
};

export default getCompanies;
